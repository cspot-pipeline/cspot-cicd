name: Run Stage 1.

on: push

jobs:
  run-stage-1:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      - name: Run script file
        run: |
         chmod +x ./stage1.sh
         ./stage1.sh
        shell: bash
